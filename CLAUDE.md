# Holy Echoes — Claude Notes

## Python Utilities

Image processing scripts live in `python_utils/` with a local venv at `python_utils/venv/`.
Run scripts with `python_utils/venv/bin/python python_utils/<script>.py`.

- `resize_splash_logo.py` — resizes the splash screen logo across Android and iOS assets.